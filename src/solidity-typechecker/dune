; generated by drom from package skeleton 'library'

(library
  (name solidity_typechecker)
  (public_name solidity-typechecker)
  (wrapped false)
  (libraries uutf solidity-parser solidity-common hacl-star ez_file )
  
  (private_modules
        solidity_c3
  )

  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package solidity-typechecker))


