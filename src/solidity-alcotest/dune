; generated by drom from package skeleton 'driver'
(executable
  (name main)
  (public_name solidity-alcotest)
  (package solidity-alcotest)
  (libraries solidity-typechecker solidity-parser solidity-common alcotest )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package solidity-alcotest))


