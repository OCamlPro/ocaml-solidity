; generated by drom from package skeleton 'library'

(library
  (name ocaml_solidity_common)
  (public_name ocaml-solidity-common)
  (wrapped false)
  (libraries zarith hex )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package ocaml-solidity-common))


