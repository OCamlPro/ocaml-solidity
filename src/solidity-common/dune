; generated by drom from package skeleton 'library'

(library
  (name solidity_common)
  (public_name solidity-common)
  (wrapped false)
  (libraries zarith hex fileutils ez_file )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package solidity-common))


